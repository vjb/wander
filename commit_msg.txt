refactor: move 'avoid steep slopes' toggle into Advanced Filters accordion

Was floating as a standalone row between the accordion and companion selector.
Now lives as the last item inside the accordion panel, after 'free only',
with a matching slope-chart icon and the same visual style as other options.

Props added to AdvancedFiltersAccordion: avoidSlopes, setAvoidSlopes.

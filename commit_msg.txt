feat: optimise waypoint order via Google Distance Matrix API

Adds _optimize_waypoint_order() which fires a single square Distance
Matrix call in walking mode, then brute-forces all N! permutations
to find minimum total walking time. Silently falls back to original
LLM order on any failure. Re-numbers waypoints after reorder.
Logs time saved when more than 30s improvement found.
